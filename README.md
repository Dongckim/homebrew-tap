# homebrew-tap

Homebrew formulae for [A.P.E](https://github.com/Dongckim/A.P.E) and other projects.

## Install

```bash
brew tap dongckim/tap
brew install ape
```

Or directly:

```bash
brew install dongckim/tap/ape
```
